# MoveDot Data Analytics Agent - Environment Variables
# Copy this file to .env and configure as needed

# ============================================================================
# Google OAuth (Required for authentication)
# ============================================================================
# Get these from Google Cloud Console: https://console.cloud.google.com/
# Create OAuth 2.0 credentials and set authorized redirect URIs
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback

# ============================================================================
# LangSmith (Optional - for tracing and monitoring)
# ============================================================================
# Get your API key from: https://smith.langchain.com/
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=movedot-analytics-agent

# ============================================================================
# Application Configuration (Optional - defaults are usually fine)
# ============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Data directory for storing files
DATA_DIR=data

# Database path
APP_DB_PATH=data/app.db

# CSV memory file
CSV_MEMORY_FILE=csv_memory.json

# API endpoints file
API_ENDPOINTS_FILE=_api_endpoints.txt

# E2B Sandbox timeout in seconds (default: 1800 = 30 minutes)
E2B_SANDBOX_TIMEOUT=1800

# Agent temperature (default: 0.1)
# Note: Users configure temperature in the web interface.
AGENT_TEMPERATURE=0.1

# ============================================================================
# IMPORTANT NOTES:
# ============================================================================
# - OpenAI, Anthropic, and E2B API keys are NOT configured here
# - These keys are configured per-user via the web interface Settings
# - Each user must configure their own API keys after logging in
# ============================================================================
