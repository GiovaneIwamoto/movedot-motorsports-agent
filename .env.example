# =============================================================================
# MoveDot Data Analytics Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: API Keys
# -----------------------------------------------------------------------------

# OpenAI API Key (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# E2B API Key (OPTIONAL but RECOMMENDED for code execution)
# Get your API key from: https://e2b.dev/
# Required for analyze_data_with_pandas tool to work
E2B_API_KEY=your-e2b-api-key-here

# -----------------------------------------------------------------------------
# OPTIONAL: Alternative LLM Provider
# -----------------------------------------------------------------------------

# Anthropic API Key (OPTIONAL - alternative to OpenAI)
# Get your API key from: https://console.anthropic.com/
# Note: API keys are typically configured via web interface, but can be set here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# -----------------------------------------------------------------------------
# OPTIONAL: LangSmith Tracing & Observability
# -----------------------------------------------------------------------------

# LangSmith API Key (OPTIONAL - for tracing and debugging)
# Get your API key from: https://smith.langchain.com/
LANGSMITH_API_KEY=your-langsmith-api-key-here

# LangSmith Project Name (OPTIONAL - defaults to "movedot-analytics-agent")
LANGSMITH_PROJECT=movedot-analytics-agent

# -----------------------------------------------------------------------------
# OPTIONAL: Model Configuration
# -----------------------------------------------------------------------------

# Agent Model (OPTIONAL - defaults to "gpt-4o-mini")
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo, etc.
AGENT_MODEL=gpt-4o-mini

# Agent Temperature (OPTIONAL - defaults to 0.1)
# Lower values = more deterministic, Higher values = more creative
# Range: 0.0 to 2.0
AGENT_TEMPERATURE=0.1

# -----------------------------------------------------------------------------
# OPTIONAL: File Paths & Directories
# -----------------------------------------------------------------------------

# Data Directory (OPTIONAL - defaults to "data")
# Directory where CSV files and other data are stored
DATA_DIR=data

# CSV Memory File (OPTIONAL - defaults to "csv_memory.json")
# JSON file that stores cached CSV data
CSV_MEMORY_FILE=csv_memory.json

# API Endpoints File (OPTIONAL - defaults to "_api_endpoints.txt")
# File that stores discovered API endpoints
API_ENDPOINTS_FILE=_api_endpoints.txt

# App Database Path (OPTIONAL - defaults to "data/app.db")
# SQLite database for storing user sessions and conversations
APP_DB_PATH=data/app.db

# -----------------------------------------------------------------------------
# OPTIONAL: E2B Sandbox Configuration
# -----------------------------------------------------------------------------

# E2B Sandbox Timeout (OPTIONAL - defaults to 1800 seconds = 30 minutes)
# Maximum execution time for code in E2B sandbox (in seconds)
E2B_SANDBOX_TIMEOUT=1800

# -----------------------------------------------------------------------------
# OPTIONAL: Google OAuth (for user authentication)
# -----------------------------------------------------------------------------

# Google OAuth Client ID (OPTIONAL)
# Required only if you want to enable Google authentication
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id-here

# Google OAuth Client Secret (OPTIONAL)
# Required only if you want to enable Google authentication
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# Google OAuth Redirect URI (OPTIONAL)
# Required only if you want to enable Google authentication
# Format: http://localhost:8000/auth/google/callback (for local dev)
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# -----------------------------------------------------------------------------
# OPTIONAL: Logging Configuration
# -----------------------------------------------------------------------------

# Log Level (OPTIONAL - defaults to "INFO")
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# OPTIONAL: Server Configuration (for web interface)
# -----------------------------------------------------------------------------

# Server Host (OPTIONAL - defaults to 0.0.0.0)
# Use 0.0.0.0 to accept connections from any interface
HOST=0.0.0.0

# Server Port (OPTIONAL - defaults to 8000)
PORT=8000

# Debug Mode (OPTIONAL - defaults to False)
# Set to True for development, False for production
DEBUG=False
